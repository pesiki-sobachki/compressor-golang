# Установка и Запуск

Так как проект использует высокопроизводительную библиотеку `bimg`, которая является оберткой над Си-библиотекой `libvips`, для работы проекта требуется **CGO** и установленные в системе заголовочные файлы `libvips`.

## 📋 Системные требования

*   **Go:** 1.21 или выше
*   **GCC:** Компилятор C (обычно есть в Linux/macOS по умолчанию)
*   **libvips:** Версия 8.10 или выше

> **Внимание пользователям Windows:** Настоятельно рекомендуется использовать **WSL2 (Ubuntu)**. Запуск нативного `libvips` под Windows сложен и требует ручной настройки переменных окружения.

## 🐧 Установка зависимостей

### Ubuntu / Debian (включая WSL)

```bash
# Обновляем пакеты
sudo apt update

# Устанавливаем libvips и необходимые инструменты сборки
sudo apt install libvips-dev gcc
```

### macOS (Homebrew)

```bash
brew install vips
```

## 🚀 Запуск проекта

1.  **Клонируйте репозиторий:**

```bash
git clone https://github.com/pesiki-sobachki/compressor-golang.git
cd compressor-golang
```

2.  **Загрузите Go-модули:**

```bash
go mod download
```

3.  **Запустите сервер:**

```bash
go run cmd/compressor-server/main.go
```

Если все прошло успешно, вы увидите сообщение:
`Starting server on: 8080...`

## 🐛 Возможные проблемы

**Ошибка: `bimg.h: No such file or directory`**
Это означает, что в вашей системе не установлена библиотека `libvips-dev`. Вернитесь к пункту "Установка зависимостей".

**Ошибка: `pkg-config: exec: "pkg-config": executable file not found`**
Убедитесь, что у вас установлен пакет `pkg-config` (обычно входит в состав dev-tools в Linux).