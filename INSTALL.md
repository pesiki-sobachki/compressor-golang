## 🧱 Общая информация

Этот проект использует высокопроизводительную библиотеку Go `bimg`, которая является обёрткой над C‑библиотекой `libvips`.
Из-за этого для работы сервиса необходим включённый **CGO** и установленные в системе заголовочные файлы `libvips`.

## 📋 Системные требования

- **ОС:** Linux (рекомендуется Ubuntu / Debian, включая WSL2)
- **Go:** версии 1.21 и выше
- **GCC / build-essential:** инструменты сборки C для CGO
- **libvips:** версии 8.10 и выше

> **Пользователям Windows:** настоятельно рекомендуется использовать **WSL2 с Ubuntu**.
> Нативный запуск `libvips` под Windows сложен и требует ручной настройки переменных окружения и путей к DLL.

## 🐧 Установка зависимостей (Ubuntu / Debian / WSL2)

Все необходимые системные и Go‑зависимости можно поставить одной командой через `make`:

```bash
# Установка системных пакетов (libvips, инструменты сборки) и Go-модулей
make deps
```

Внутри эта цель выполняет примерно следующее:

- Обновляет список пакетов `apt`.
- Устанавливает `build-essential`, `libvips`, `libvips-dev`.
- Запускает `go mod tidy` и `go mod download` для загрузки Go‑модулей.

Если хочется сделать всё вручную, можно выполнить эквивалентные команды:

```bash
sudo apt update
sudo apt install -y build-essential libvips libvips-dev

go mod tidy
go mod download
```


## 🚀 Запуск проекта

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/pesiki-sobachki/compressor-golang.git
cd compressor-golang
```

2. **Установите зависимости (если ещё не установлены):**
```bash
make deps
```

3. **Соберите бинарник:**
```bash
make build
```

После этого скомпилированный бинарный файл появится в директории `./bin/api`.

4. **Запустите сервер:**
```bash
make run
```

По умолчанию HTTP‑сервер слушает порт `8080` (или тот порт, который задан в настройках приложения).

## 🐛 Возможные проблемы

**Ошибка:** `bimg.h: No such file or directory`

- В системе не установлены заголовки `libvips`.
- Установите пакеты `libvips` и `libvips-dev`:

```bash
sudo apt install -y libvips libvips-dev
```

**Ошибка:** `pkg-config: exec: "pkg-config": executable file not found`

- Не установлен пакет `pkg-config`.
- Установите его:

```bash
sudo apt install -y pkg-config
```

После устранения ошибок можно снова выполнить:

```bash
make deps
make build
make run
```